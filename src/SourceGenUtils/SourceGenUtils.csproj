<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>netstandard2.0</TargetFramework>
        <IsPackable>true</IsPackable>
        <Nullable>enable</Nullable>
        <LangVersion>latest</LangVersion>

        <EnforceExtendedAnalyzerRules>true</EnforceExtendedAnalyzerRules>
        <IncludeBuildOutput>false</IncludeBuildOutput>

        <RootNamespace>Hertzole.SourceGenUtils</RootNamespace>
        <PackageId>Hertzole.SourceGenUtils</PackageId>
        <AssemblyName>SourceGenUtils</AssemblyName>

        <Authors>Hertzole</Authors>
        <Company>Hertzole</Company>
        <AssemblyVersion>1.0.0.0</AssemblyVersion>
        <Title>SourceGenUtils</Title>
        <PackageProjectUrl>https://github.com/Hertzole/sourcegenutils</PackageProjectUrl>
        <PackageLicenseExpression>MIT</PackageLicenseExpression>
        <RepositoryUrl>https://github.com/Hertzole/sourcegenutils</RepositoryUrl>
        <RepositoryType>git</RepositoryType>
        <PackageTags>sourcegen, source, source generator, incremental, incremental generator, util, utility, utils, string, stringbuilder, builder, string builder, code, codewriter, code writer</PackageTags>
        <NeutralLanguage>en-US</NeutralLanguage>
        <Version>1.0.0-rc.1</Version>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Configuration)' == 'Release' ">
        <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
        <PackageReadmeFile>README.md</PackageReadmeFile>
        <NoWarn>NU5128</NoWarn> <!-- Ignore warning about no files in libs folder. Generator publishes to analyzer folder. -->
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Microsoft.CodeAnalysis.CSharp" Version="4.14.0" PrivateAssets="all"/>
    </ItemGroup>

    <ItemGroup>
        <None Include="../../README.md" Pack="true" PackagePath="\"/>
    </ItemGroup>

    <ItemGroup Condition="'$(Configuration)' == 'Release'">
        <!-- Package the generator in the analyzer directory of the nuget package -->
        <None Include="$(OutputPath)\$(AssemblyName).dll" Pack="true" PackagePath="analyzers/dotnet/cs" Visible="false"/>
    </ItemGroup>

</Project>


